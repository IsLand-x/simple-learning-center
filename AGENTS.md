# Learning Center 项目协作规范

本文件适用于仓库根目录及其所有子目录。修改项目之前先阅读本文件，并保持现有本地优先架构与交互约定。

## 项目定位

- 这是一个本地优先的个人 EPUB 阅读与 AI 学习 Web App，配套单用户 Node 数据服务。
- 当前一级功能是“读书”和“设置”；不要擅自增加多用户账号、云同步或无关一级栏目。
- 数据服务同时支持仅本机访问和带认证的远程访问；不要擅自扩展为多用户平台。
- 默认只进行本地开发、构建和验证。除非用户明确要求，不要部署、发布或修改远程环境。
- 所有新增文案默认使用简体中文，技术标识、协议名和代码除外。

## 技术栈与命令

- React 18、TypeScript、Vite 8。
- UI 使用 Semi Design 和 Semi Icons。
- EPUB 运行时渲染、分页、手势、标注层和书内搜索索引使用 Foliate.js；epub.js 仅保留用于导入元数据与封面。
- 全局状态使用 Zustand；持久化使用 Zustand `persist`。
- 可调整分栏使用 Allotment，不要重新引入自制拖拽分隔条。
- PWA 使用 `vite-plugin-pwa`。
- 服务端使用 Node.js 内置 HTTP 与文件系统 API，不依赖外部数据库服务。

常用命令：

```bash
npm run dev
npm run build
npm start
npm run preview
```

完成代码修改后至少执行：

```bash
npm run build
git diff --check
```

构建中的第三方依赖警告可以记录，但不能忽略 TypeScript、Vite 或 PWA 构建失败。

## 目录职责

- `src/pages/`：页面级路由与功能编排。
- `src/components/`：可复用界面、阅读器和侧边栏组件。
- `src/lib/`：EPUB、服务端存储适配、旧浏览器数据迁移、字体和 AI 任务 API 等基础能力。
- `server/`：静态站点、本地数据 API、远程访问认证和开发启动编排。
- `src/store/`：跨页面状态、持久化和数据迁移。
- `src/types.ts`：共享领域类型。
- `src/styles.css`：全局样式、Semi Design 覆盖和明暗主题适配。
- `public/`：PWA 图标和无需编译的静态资源。
- `data/`：运行时创建的用户数据目录，禁止提交到 Git。

## 本地数据与安全

- 默认数据目录是仓库根目录下的 `data/`，可通过 `LEARNING_CENTER_DATA_DIR` 指向其他路径。
- EPUB 文件保存在 `data/books/`，书内搜索索引保存在 `data/search-indexes/`，Markdown 笔记正文保存在 `data/notes/`。
- 元数据、阅读进度、高亮、评论、AI 对话、模型配置和笔记元数据保存在 `data/state.json`。
- `localStorage` 与 IndexedDB 的旧数据只用于服务端空目录首次启动时的自动迁移；迁移后不得继续作为日常持久化来源。
- 修改持久化数据结构时必须提升 store 版本并提供向后迁移，不能让已有本地数据静默丢失。
- 删除书籍时必须同步清理 EPUB 文件及该书关联的笔记、高亮、评论、对话和阅读记录。
- 状态文件和数据文件默认使用仅当前系统用户可读写的权限；不要放宽权限或把数据目录放入静态资源目录。
- API Key 保存在服务端数据目录。不要写入源码、构建产物、日志、测试快照或提交记录。
- 不要把书籍正文、笔记、高亮、评论、阅读记录或 API Key 发送给未被用户选择的第三方服务。
- 不要使用破坏性命令清理用户工作区或浏览器数据。
- 本地模式固定绑定 `127.0.0.1`；远程模式必须设置 `LEARNING_CENTER_PASSWORD`，并在公开网络前使用 HTTPS 反向代理。

## 阅读器约定

- EPUB 的准确位置以 CFI 为主，百分比和页码用于展示；恢复阅读进度时不能只跳到章节开头。
- 页面刷新或重新进入书籍时，应直接恢复最后阅读页面，避免先显示 0% 再跳到已保存进度。
- 高亮必须绑定 EPUB CFI 或稳定文本位置，不能依赖屏幕像素坐标；调整面板宽度后不能错位。
- 删除高亮时必须同步删除阅读器中的视觉高亮及其评论。
- 选区操作使用“评论”直接记录见解，不再展示独立“笔记”入口；评论型正文标记不进入普通高亮列表。
- 带评论的正文高亮使用 Semi Icons 的实心三点气泡 `IconComment` large 图标，图标中心定位在高亮最后一行最后一个文字矩形的右上角；角标背景使用完整的当前阅读高亮色，图标使用当前阅读正文色，普通高亮删除评论时保留高亮，评论型高亮删除评论时同步删除正文标记。
- 持久高亮使用 Foliate.js Overlayer 根据 CFI 对应 Range 的文字矩形绘制，保持主题高亮色；高亮块高度取文字矩形与当前阅读行高中的较大值并上下居中扩展，使选中状态与保存后的视觉行高一致。不要做像素坐标缓存或延迟计时器几何修正。
- 点击持久高亮的任意可见区域时展示“取消高亮”“在高亮中查看”和评论操作；点击命中范围必须与扩展后的视觉高亮一致，不能只响应原始字形矩形。
- 翻页动画、触屏拖动、分页吸附和连续操作锁使用 Foliate.js Paginator 的 `animated`、`scrollBy`、`snap`、`next` 和 `prev`，不要叠加全局 View Transition 或自制双页面动画。
- 桌面 Chromium 对 Blob iframe 的兼容由 `vite.config.ts` 的 Foliate srcdoc 变换与 `src/lib/foliateReader.ts` 的安全章节通道共同处理；升级 Foliate.js 时必须重新验证该变换，不要直接修改 `node_modules`。
- 新建或编辑评论时正文标记保持高亮，支持 `Cmd/Ctrl + Enter` 保存，并在保存按钮 Tooltip 中提示快捷键。
- 字体设置必须同时应用于演示正文和 EPUB iframe 内的正文。
- 阅读样式预设与应用浅色/暗色模式相互独立；预设和自定义参数必须同时应用于演示正文与 EPUB iframe。
- 商业系统字体只使用本机字体栈，不要把未获再分发许可的字体文件提交到仓库。
- `←` / `↑` 控制上一页，`→` / `↓` 控制下一页；编辑输入框时不得劫持方向键。
- 目录跳转与上一页/下一页必须共享同一导航队列，不能让 Foliate 分页动画锁静默丢弃目录点击；目录 href 无法直接对应时使用 Foliate 目录 href 和章节名称兜底解析。
- 目录只展示章节文字与层级，不在每行显示图标或阅读进度条；当前章节仍需有明确高亮并自动滚动到可见区域。

## AI 助手约定

- AI 通过服务端的 Vercel AI SDK OpenAI Compatible Provider 请求用户配置的 `/chat/completions` 端点；浏览器只创建、查询和取消服务端 AI 任务，不得重新直连模型供应商。
- 服务端 AI 任务在浏览器关闭或网络暂时断开后继续运行，完成结果写入数据目录；应用服务或容器重启时允许中断尚未完成的内存任务，并应向用户明确这一限制。
- 不要重新引入 ACP、Codex CLI、Kimi CLI 桥接服务器或相关启动脚本。
- 模型配置支持多个供应商和多个模型；选择器先选供应商，再选模型，收起状态只展示模型名称。
- 当前书籍、章节、笔记、高亮、评论、阅读轨迹、书内搜索、段落读取、联网搜索和网页读取默认作为 Agent 工具提供，避免增加手动勾选上下文的下拉框。
- 书内搜索必须使用服务端数据目录中的 EPUB 分段索引，不要为了建立索引把整本书发送给模型或第三方服务。
- 联网搜索与网页读取使用单独的搜索服务配置，不能假设模型 API Key 自带联网能力；搜索词和目标 URL 的外发行为必须在设置页明确提示。
- 多步工具调用使用 AI SDK 的 `stopWhen` 设置明确上限，防止模型陷入无休止调用；工具必须使用 schema 校验参数。
- 选中文字提问时，原文必须作为独立引用标签发送和展示，不能拼接成普通用户输入。
- 使用 Semi Design AI Chat 组件和 OpenAI 消息格式展示对话。
- 如果端点返回思考字段或工具调用，应该展示真实过程；不要用固定的“模型正在思考”文案冒充过程。
- 对话历史保存在服务端数据目录。点击 AI 按钮开启新对话；历史对话可以继续；右键菜单可以删除对话。
- 删除模型或对话时，提示应明确说明只删除服务器数据目录中的数据。

## 前端视觉规范

本节是新增页面、组件和状态样式的视觉基准。先复用最接近的现有页面或组件，再扩展样式；通用 UI 模板、第三方设计建议或个人偏好不能覆盖这里已经形成的项目风格，除非用户明确要求整体改版。

### 视觉基调与表面层级

- 整体气质是克制、紧凑、内容优先的阅读工作台，不是营销网站。不要新增 Logo、“学习中心”品牌横幅、Hero、大段宣传文案、高饱和装饰、玻璃态、拟物化或无功能意义的渐变色块；登录页现有的主题表面渐变是例外。
- 页面底层和主内容区使用 `--semi-color-bg-0`；导航、标题栏、工具栏、侧栏和页面级卡片通常使用 `--semi-color-bg-1`；嵌入 `bg-1` 面板内的笔记、高亮、评论等卡片使用 `bg-0` 形成一级层次；Popover、正文选区工具栏和评论编辑器等真正悬浮的表面使用 `--semi-color-bg-2`。
- 独立卡片依靠 `1px solid var(--semi-color-border)`、背景层级和间距区分，不使用常驻重阴影；目录、RSS 来源、笔记列表等行式内容默认无边框，靠背景和选中态区分。`--semi-shadow-elevated` 只用于登录卡、Popover、SideSheet、覆盖式目录和其他确实离开文档平面的浮层。
- 相邻面板之间只保留一条必要的 1px 分隔线；隐藏或折叠面板时同步隐藏对应分隔线，禁止双线、多层描边和卡片套卡片。
- 圆角必须复用 Semi token：列表项和自制小按钮使用 `--semi-border-radius-small`，自制卡片、编辑器容器和浮层通常使用 `--semi-border-radius-medium`，登录卡或大型模态才使用 `--semi-border-radius-large`；原生 Semi 输入与按钮保留组件默认圆角。`9999px` 只用于标签、上下文与选择器等胶囊控件，`50%` 只用于圆形标记；不要把所有容器做成大圆角。
- 书籍封面保留“书脊较直、右侧稍圆”的形态：桌面为 `3px 8px 8px 3px`，移动端为 `4px 8px 8px 4px`；底部阅读抽屉保留 `20px 20px 0 0`，这些是功能语义明确的例外，不扩散到普通卡片。

### 主题、颜色与排版

- 应用界面颜色必须来自 Semi Design 语义变量。正文、次级信息、弱化元信息分别使用 `--semi-color-text-0`、`--semi-color-text-1`、`--semi-color-text-2`；主操作和选中态使用 `--semi-color-primary` 及对应的 `primary-light-*` 变量。
- 除阅读主题预设、透明遮罩、阴影和必要的 `color-mix()` 外，不新增硬编码黑白色、灰阶或品牌色，也不要创建一套与 Semi 并行的应用配色 token。
- 浅色和深色模式通过 Semi token 与 `body[theme-mode]` 适配；新增颜色、图标、输入框、图表和浮层必须同时验证两种模式。禁止写死黑色图标、白色浮层文字或只在单一主题可见的边框。
- 应用主题与阅读纸张主题相互独立。阅读颜色由 `resolveReaderStyle` 统一解析并注入；现有 CSS token 包括 `--reader-paper-color`、`--reader-muted-color`、`--reader-accent-color`、`--reader-highlight-color`、`--reader-callout-color` 等，不能把应用的浅色/暗色模式强行同步给阅读预设。
- 应用界面使用现有中文无衬线本机栈：`"PingFang SC", "Microsoft YaHei", "Noto Sans CJK SC", system-ui, sans-serif`。按钮、输入框和正文继承该字体；代码、版本号和技术值使用 Semi 的 code/monospace token。不要为应用外壳引入新的 Web 字体。
- 页面和区块标题优先使用 Semi `Typography` 的既有 `Title heading={4|5}` 层级，不另造大标题。密集元信息和底部导航通常为 `10–12px`，卡片标签和列表标题为 `13–14px`，常规正文、编辑器和聊天为 `14px`，长文正文与移动端输入为 `16px`。
- 行高随内容密度变化：紧凑控件约 `1.15–1.35`，普通列表与聊天约 `1.45–1.5`，编辑器、评论和长文约 `1.65–1.78`。不要把阅读正文的舒展行高套用到应用 UI。
- 单行标题和元信息使用 ellipsis，摘要使用 2 行 clamp，高亮摘录最多 4 行；长内容不能撑宽面板。进度、页码等数字优先使用 tabular numerals。
- 阅读正文继续复用三档密度：紧凑为 `line-height: 1.6 / 段距 1em / 页边距 4%`，适中为 `1.8 / 1.25em / 7%`，舒展为 `2 / 1.45em / 10%`；不要在页面组件中复制另一套阅读排版常量。

### 布局、尺寸与间距

- `html`、`body`、`#root` 和 `.app-shell` 固定占满视口并保持 `overflow: hidden`；滚动下沉到页面内容区或具体面板。所有参与 flex/grid 分栏的中间层必须保留 `min-width: 0` 和 `min-height: 0`，避免内容撑破布局。
- 桌面左侧主导航默认宽 `180px`，折叠宽 `60px`；不在导航顶部增加品牌区。底部工具依次保留 PWA 安装、主题切换和功能栏折叠按钮。
- 普通页面沿用居中内容骨架：书架最大宽 `1180px`，设置最大宽 `880px`，桌面页面 padding 使用 `24px 28px` 量级。长文正文限制行宽，当前 RSS 正文最大 `760px`、标题与摘要最大 `820px`，禁止在宽屏上横向铺满。
- 工作台页面（阅读器、RSS 等）使用 `overflow: hidden` 的全高分栏，各栏内部独立滚动；可调整分栏继续使用 Allotment，不实现第二套拖拽分隔条。
- 桌面阅读器标题栏、阅读工具栏、目录标题栏和右面板标题栏统一使用 `--reader-bar-height: 44px`；栏内通常左 padding `12px`、右 padding `8px`、控件 gap `6–8px`。RSS 栏内标题和详情工具栏也为 `44px`，页面级 RSS 总标题栏为 `52px`，不要混淆两个层级。
- 阅读目录宽度限制为 `220–400px`，右侧辅助面板为 `320–720px`；RSS 订阅源栏为 `160–340px`（默认 `220px`），内容列表为 `190–520px`（默认 `320px`），右侧辅助面板为 `320–720px`。新增分栏应复用相近量级并为主内容保留空间，不使用无上下限的固定宽栏。
- 右侧辅助功能栏固定宽 `52px`，内部按钮为 `44×44px`，栏内 padding 与按钮 gap 均为 `4px`；保持图标在上、文字在下，图标约 `18px`、文字 `10px`。
- 间距优先复用当前节奏：微间距 `2/4/6/8px`，组件内间距 `10/12/14/16px`，区块间距 `20/24/28/32px`。现有 `3/5/7/9/17/18px` 等光学校准值可以保留，但不要机械扩散或为了“统一”重写；密集侧栏使用 `6–12px` 内边距，不在列表中引入 24px 以上的大留白。
- 卡片只承载独立内容单元。桌面书籍卡片使用 `min-height: 178px; padding: 16px; gap: 16px`，设置摘要约 `min-height: 64px; padding: 10px 12px`；面板列表应优先使用透明列表行，而不是把每一行都包装成带阴影卡片。
- 页面级空状态使用 Semi `Empty` 并在约 `360px` 的稳定区域居中；面板级空状态按面板自然居中或使用约 `24px` 内边距。文案保持一句标题和必要的一句下一步说明，不新增品牌插画或宣传卡片。

### 组件、图标与交互状态

- 优先复用 Semi Design 组件。主要提交或新建操作使用主色实心按钮；取消和低权重工具使用 `borderless tertiary`；删除使用 `danger`；筛选使用 `ButtonGroup`，当前项 solid、其余 borderless。
- 图标统一使用 Semi Icons，颜色继承 `currentColor`。禁止混用 Emoji、字符图标或另一套图标库；桌面辅助栏图标约 `18px`，移动导航图标约 `19–20px`。
- 纯图标操作必须使用 Semi `Button`，提供明确 `aria-label`；图标含义不能从上下文直接判断时增加 Tooltip。不要用带 `onClick` 的无语义 `div` 代替按钮。
- 默认文本、悬停和选中必须保持同一状态语义：默认使用 `text-1/text-2`，hover 使用 `fill-0` 或浅主色背景，active/selected 使用 `fill-1` 或 `primary-light-default` 并配主色文字/边框；危险与 disabled 状态交给 Semi 语义，不自行用任意透明度模拟。
- 列表选中不能只靠颜色：目录保留左侧 `3px` 标记，卡片使用边框或浅主色表面；按钮在语义适用时补充 `aria-pressed` 或 `aria-current`。交互项 hover 不使用缩放、上浮或改变尺寸。
- 自定义可交互元素必须有可见的 `:focus-visible`，使用 2px 的 `--semi-color-focus-border`、`--semi-color-primary-light-active` 或 `--semi-color-primary-light-default` 内外描边。若写 `outline: none`，必须同时提供等价焦点样式。
- 桌面列表行通常保持 `34–40px` 的紧凑导航高度，内容摘要行约 `58–76px`；主要触控操作不得照搬这些紧凑高度。
- 表单继续使用可见 label、清晰的 loading/disabled/error 状态。新增移动端文本输入统一使用 `16px`，并逐步补齐旧输入，避免 iOS 自动缩放；异步提交期间禁止重复点击。
- 自定义非原生交互元素确实无法改用 Button 时，必须同时提供适当的 `role`、`tabIndex`、Enter/Space 键盘操作和可见焦点。
- 正文选区、高亮操作浮层使用 Semi `ButtonGroup`、`bg-2`、主题变量和 elevated shadow，不展示指向箭头；亮暗主题下保持同一信息层级。

### 响应式与移动端

- `800px` 是进入移动视觉模式的唯一主断点，CSS 与 `matchMedia('(max-width: 800px)')` 行为判断必须一致。`860px` 只用于配置表单折行，`700px` 是 RSS 紧凑工作区的 viewport 判断，`560/480px` 用于极窄屏微调；现有 `720px` 规则只是局部回退且部分会被后置的 `800px` 规则覆盖，不要继续把新移动规则堆入其中。
- 移动端完全隐藏桌面左导航，普通页面改用底部一级导航；阅读页不显示全局底栏，而使用专用阅读工具栏。底栏项目等宽，图标在上、文字在下。
- 移动全局底栏高度为 `64px + env(safe-area-inset-bottom)`；移动阅读顶栏为 `58px`，底部阅读工具栏为 `58px + safe-area`，并在 `.reader-page` 中预留等高空间，禁止直接覆盖正文。
- 移动阅读工具栏固定为目录、上一页、下一页和更多功能四等分，实际按钮高 `50px`。全局底栏、阅读工具栏和面板标签统一使用图标行 `20px`、文字行 `14px` 的网格轨道与 `2px` 间距；实际文字字号分别约为 `12px`、`11px`、`10px`，不依赖组件库默认 icon margin。
- 新增移动端主要触控目标不得小于 `44×44px`；相邻主要操作保留清晰间距。现有选区工具栏按钮 `42px`、目录项 `40px` 属于紧凑例外，不复制到新的主操作；其他视觉尺寸不足 44px 时通过明确的伪元素命中区补足。
- 固定底栏及抽屉内贴边操作区必须按控件所在边缘计入对应的 `env(safe-area-inset-*)`，内容区同步预留空间，不能只在外层追加一块空白。
- 移动书架使用两列封面流，而不是缩小桌面卡片：页面 padding `14px 14px 20px`，列 gap `18px 12px`，卡片取消边框、背景与内边距，封面保持 `3:4`。
- 移动目录使用无关闭叉的左侧 Semi SideSheet，宽 `min(86vw, 340px)`，上下避开阅读顶栏和底栏；阅读辅助面板使用底部 SideSheet，高 `90dvh`、顶角 `20px`，带 `36×4px` 抓手，通过遮罩或原入口收起。
- 移动端展示 Modal、SideSheet、Popover、图片查看器等弹层时，系统返回键和浏览器返回操作通常必须优先关闭最上层弹层；只有没有可关闭弹层时才返回上一页面。需要让弹层占用可回退的临时历史层，关闭和切换弹层时避免产生重复或失效的历史记录。
- 底部面板内部必须使用稳定的 flex 结构：内容区 `flex: 1; min-height: 0; overflow: auto`，底部标签栏固定，并允许横向滚动与隐藏滚动条。不要让异步内容改变面板高度或挤走标签栏。
- 手势按容器职责限定：普通按钮使用 `touch-action: manipulation`，横向标签使用 `pan-x`，阅读正文使用 `pan-y` 和 `overscroll-behavior: contain`，选区手柄使用 `touch-action: none`。不要在无关容器劫持系统手势。

### 动效、浮层与层级

- 动效保持短、轻、可中断：颜色和背景反馈通常为 `160ms ease`，表面显隐与移动端内容进入通常为 `180ms ease`，微交互和内容进入位移控制在 `4–8px`；阅读 Chrome 与 SideSheet 等结构性元素可沿屏幕边缘完整进出。禁止弹跳、无语义的长距离滑动、全页 View Transition 或持续超过 300ms 的装饰动画。
- 显隐优先动画 `opacity` 和 `transform`，不要动画会触发布局重排的承载容器 width/height；微型状态指示线等局部视觉例外可以动画自身尺寸。移动阅读 Chrome 使用 opacity/transform/visibility 隐藏并同步禁用 pointer events，同时保留正文分页区域。
- 保持现有自定义阅读器层级栈：阅读宿主 `1`、状态层 `2`、Allotment sash `8`、桌面目录浮层 `18`、移动选区手柄 `24`、选区工具栏和阅读底栏 `30`、评论编辑器 `31`、SideSheet `38`、移动全局导航 `40`、PWA 标题栏拖动区 `1000`。新增自定义浮层应插入合理层级，不随意使用超大 z-index；Semi Modal、Toast、Dropdown、Popover 等 portal 组件继续使用组件库自身层级。
- 所有动画必须继续受全局 `prefers-reduced-motion: reduce` 约束；不要为新组件绕开或覆盖该降级规则。
- 浮层、拖拽、图片和异步内容要预留稳定空间，避免明显布局跳动。Foliate 容器继续使用 contain 与合成层优化，不在其外叠加自制页面动画。

### 样式实现与视觉验收

- JSX 默认使用功能范围内的语义 class，并在 `src/styles.css` 中用 Semi token 定义样式；不要在 JSX 中堆叠 Tailwind utility、复制长串 inline 静态样式或引入另一套 CSS-in-JS 方案。动态坐标、阅读主题色和数据驱动预览可使用 inline style。
- class 命名延续现有 `block__element--modifier` 语义；覆盖 Semi 内部样式必须由页面或组件父 class 限定，禁止无作用域污染全局组件。
- 新组件先复用已有视觉原型：普通页面参考书架/设置，分栏工作台参考阅读器/RSS，右侧工具参考 `ActivityRailButton`，移动抽屉参考 `ReaderMobileChrome`，浮层参考正文选区工具栏。
- 没有明确需求时，不新增新的颜色 token、阴影级别、圆角等级、断点或动画曲线；确需新增时说明现有规范为何无法覆盖，并保持可复用。
- 视觉调整至少检查桌面浅色、桌面深色、移动浅色、移动深色四种组合，并覆盖 `375px`、`768px`、`1024px`、`1440px` 代表宽度。
- 验收同时检查：键盘焦点、44px 触控目标、长文本省略、空状态、loading/disabled/error、虚拟键盘、安全区、无横向溢出和 `prefers-reduced-motion`。

## 交互与组件行为规范

- 左侧功能栏按钮保持左对齐；底部依次放置 PWA 安装、主题切换和功能栏折叠按钮。
- 右侧辅助栏按钮采用上方图标、下方文字、居中布局；默认灰色，选中后图标和文字变为主题蓝色。
- 所有面板不展示右上角关闭叉号；通过对应侧栏按钮再次点击、遮罩或返回操作收起。
- 文本选区消失时，提问/高亮/评论浮动工具栏必须自动隐藏。
- 所有二次确认弹窗统一使用 `src/lib/confirmDialog.ts` 的 `confirmDialog`，默认聚焦确认按钮，使 `Enter` 等同确认并保留 `Escape` 取消；不要直接新增 `Modal.confirm`。
- 评论编辑器打开期间，虚拟键盘导致的 visual viewport resize 不能被视为外部关闭操作；输入框应跟随可视视口定位。

## 指针右键菜单

- 右键或其他指针操作打开的菜单，必须使用事件的视口坐标 `clientX`、`clientY` 作为菜单左上角原点。
- 不要把菜单锚定在整行、卡片或按钮边缘，也不要使用会产生明显偏移的预设位置。
- 菜单应立即出现，不添加入场动画或人为延迟。组件库要求锚点时，可以使用固定定位的零尺寸锚点。
- 保留视口边缘防溢出，但在菜单能够正常容纳时必须保持精确鼠标原点。
- 点击外部、按下 `Escape`、窗口尺寸变化或祖先容器滚动时关闭菜单。

## 代码修改原则

- 保持 TypeScript 类型完整，避免新增无必要的 `any`、重复状态或并行实现。
- 优先复用现有 Semi Design、Allotment、Zustand 和 Foliate.js 能力；epub.js 只用于导入元数据与封面，不要重新接回运行时阅读器或索引。
- 修改 Semi Design 内部样式时使用功能范围内的父级 class 限定，避免污染全局组件。
- 不要直接修改 `node_modules` 或 `dist`；`dist` 必须由构建命令生成。
- 保留用户已有的未提交修改。只编辑当前任务需要的文件，除非用户明确要求整理并提交整个项目。
- 新功能完成后同步更新 README 中的功能、配置方式、数据保存或限制说明。

## Git 与提交

- 提交前检查 `git status`、`git diff --check` 和 `npm run build`。
- 不提交 `data/`、API Key、个人 EPUB、缓存、临时日志或编辑器配置。
- commit message 应简洁描述用户可感知的结果。
- 不执行强制推送、历史重写或远程发布，除非用户明确授权。
