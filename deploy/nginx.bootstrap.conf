server {
    listen 80;
    listen [::]:80;
    server_name learning-center.island-x.autos;

    location ^~ /.well-known/acme-challenge/ {
        root /var/www/letsencrypt;
        default_type text/plain;
    }

    location / {
        return 404;
    }
}
