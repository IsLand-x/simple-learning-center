# 重构任务

## 1. 基线与工程门禁

- [x] 记录当前文件热点、路由、API、状态分区和技术栈。
- [x] 运行重构前 `npm run verify`，确认 58 项测试和生产构建通过。
- [x] 增加前端单元测试、静态检查和格式检查。
- [x] 增加模块边界、未使用代码与依赖检查。
- [x] 增加关键流程与视觉回归测试基础设施。

## 2. 服务端模块化

- [x] 从 `server/app.mjs` 拆出通用 middleware。
- [x] 按 auth、state、rss、video、ai、books 拆分路由注册器。
- [x] 保持 HTTP、Cookie、ETag 和 SSE 契约测试通过。
- [x] 将新的服务端代码纳入递归语法与 ESLint 检查。

## 3. 状态层模块化

- [x] 拆分默认值、类型、领域 action、迁移和 merge 逻辑。
- [x] 保持单一 `useLearningStore` 公共入口。
- [x] 保持 persist key、版本和服务端分区同步兼容。
- [x] 为迁移、合并和墓碑逻辑补测试。

## 4. 前端功能模块化

- [x] 提取 Library 展示与书单管理组件。
- [x] 提取 Settings 配置区块。
- [x] 提取 Video 列表、正文和侧栏。
- [x] 提取 RSS 纯函数、面板、来源管理和文章工作区。
- [x] 提取 Reader 会话、导航、标注和面板编排。
- [x] 拆分 Foliate 选区、导航、手势和 Overlayer 适配器。

## 5. 样式与文档

- [x] 将全局样式按基础、外壳和领域拆分，保持入口与选择器兼容。
- [x] 验证桌面/移动、浅色/深色、焦点、触控和 reduced motion。
- [x] 更新 README 的架构、目录和验证说明。

## 6. 完成验证

- [x] 运行完整测试和生产构建。
- [x] 运行 `git diff --check` 并检查未提交文件。
- [x] 检查 API、数据与安全约束无回退。
- [x] 输出重构映射、验证证据和遗留风险。
